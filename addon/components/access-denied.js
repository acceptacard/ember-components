import Component from '@ember/component';
import layout from '../templates/components/access-denied';

export default Component.extend({
    classNames: ['ui', 'two', 'column', 'centered', 'padded', 'grid'],
});
