import Component from '@ember/component';
import layout from '../templates/components/paya-footer';

export default Component.extend({
    classNames: ['ui', 'center', 'aligned', 'primary', 'inverted', 'no-radius', 'segment']
});
