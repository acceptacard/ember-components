import Component from '@ember/component';
import layout from '../templates/components/sui-loader';

export default Component.extend({
    layout,
    classNames: ['ui', 'active', 'text', 'loader']
});
